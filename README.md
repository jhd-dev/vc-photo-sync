# VC Photo Sync Utility

## @valleycollaborative/photo-sync

This utility syncs each user's badge server photos to their Freshdesk, Google, and Microsoft accounts. To use it, run `npm start` from this directory.
